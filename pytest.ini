[pytest]
testpaths = backend/tests
pythonpath = backend
markers =
    unit: fast tests, no external dependencies
    api: round trip tests requiring postgres
    e2e: full stack tests requiring postgres and minio